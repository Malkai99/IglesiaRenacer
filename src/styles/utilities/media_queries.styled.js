import { size } from "./_variables";

export const device = {
    xs: `(min-width: ${size.xs})`,
    xsL: `(min-width: ${size.xsL})`,
    sm: `(min-width: ${size.sm})`,
    md: `(min-width: ${size.md})`,
    lg: `(min-width: ${size.lg})`,
    xlg: `(min-width: ${size.xlg})`,
};